#include <iostream>
#include <string.h>
using namespace std;

int main()
{
    cin.tie(0);
    ios_base::sync_with_stdio(false);
    string x;
    while(getline(cin, x))
    {
        cout << x << "\n";        
    }
}